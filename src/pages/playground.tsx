import React from "react";
import Layout from "../components/Layout";

interface Props {}

const Playground: React.FC<Props> = () => {
  return (
    <Layout>
      <h1>This used to be Madonna's playground...</h1>
    </Layout>
  );
};

export default Playground;
